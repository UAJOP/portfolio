node_modules
.lighthouseci
.lhtmp

# React migration foundation build artifacts. dist-react/ is generated by
# `npm run build:react` and is intentionally NOT committed, so GitHub Pages can
# never publish the preview from the repository root. CI rebuilds it before
# running `npm run qa:react`.
dist-react
.react-ssr-tmp

# Portfolio content automation. The example config, the committed fixtures and the
# credential-free n8n template ARE tracked; only real credentials and local run
# artifacts are ignored.
automation/config.local.json
automation/proposals/
.env
.env.local
